
on:
  push:
    branches:
      - main
      - "releas/**"

    paths:
      - "src/**"

  pull_request:
    branches:
      - main
      - "releas/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@4.2.2


       - name: run tests
         run: npm test


~

